# Frontend sources are authored LF. Generated output lives under ignored build/
# and inherits these line endings before xmake installs it.
frontend/** text eol=lf
frontend/**/*.png binary

# World-surface game assets, generated by tools/make_world_surface_*.py. The
# material generator copies vanilla JSON verbatim (LF) and rewrites only texture
# paths, so eol=lf keeps a regeneration from showing up as a whole-file diff on
# a core.autocrlf=true checkout. The placeholder DDS is binary.
research-world-surface-assets/materials/** text eol=lf linguist-generated=true
research-world-surface-assets/textures/**  binary
