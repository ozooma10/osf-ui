{
  "id": "osfui",
  "title": "OSF UI",
  "description": "Runtime and overlay behavior for the OSF UI framework itself.",
  "version": 1,
  "groups": [
    {
		"id": "input",
      "label": "Input",
      "settings": [
        {
          "key": "toggleKey",
          "label": "Open / close key",
          "type": "key",
          "default": "F10",
          "hint": "Press to rebind the key that opens and closes the overlay."
        }
      ]
    },
    {
		"id": "interface",
      "label": "Interface",
      "settings": [
        {
          "key": "language",
          "label": "Language",
          "type": "string",
          "default": "auto",
          "maxLength": 32,
          "hint": "Use auto for Starfield's language, or enter a locale such as en, de, or pt-BR."
        },
        {
          "key": "pauseMenuEntry",
          "label": "Pause-menu entry",
          "type": "bool",
          "default": true,
          "hint": "Add a MOD MENUS entry to the game's pause menu."
        },
        {
          "key": "vanillaKeyConflicts",
          "label": "Warn about game-key collisions",
          "type": "bool",
          "default": true,
          "hint": "Badge mod hotkeys that collide with Starfield's own bindings."
        }
      ]
    }
  ]
}
