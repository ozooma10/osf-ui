{
  "title": "Starfield Web UI — Demo Settings",
  "groups": [
    {
      "label": "General",
      "settings": [
        { "key": "overlay.enabled", "label": "Enable overlay", "type": "bool", "default": true },
        { "key": "overlay.opacity", "label": "Overlay opacity", "type": "float", "min": 0.2, "max": 1.0, "step": 0.05, "default": 0.9 },
        { "key": "overlay.scale", "label": "UI scale (%)", "type": "int", "min": 50, "max": 200, "step": 5, "default": 100 }
      ]
    },
    {
      "label": "Appearance",
      "settings": [
        { "key": "theme", "label": "Theme", "type": "enum", "options": ["Dark", "Light", "Auto"], "default": "Dark" },
        { "key": "greeting", "label": "Greeting text", "type": "string", "default": "Hello, spacefarer" }
      ]
    }
  ]
}
