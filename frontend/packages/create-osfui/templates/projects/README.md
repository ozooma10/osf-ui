# Project templates

Presets: `menu-papyrus` and `menu-native`. Both emit deployable files at `mod/Data/SFSE/Plugins/OSF/UI/views/<mod>/<view>/` and use the OSF UI 2 bridge (`send`, `request`, events, retained state). Settings templates belong to OSF Settings.
