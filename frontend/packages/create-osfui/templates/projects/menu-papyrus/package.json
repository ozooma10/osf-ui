{
  "name": "__OSFUI_PROJECT_NAME__",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "osfui dev",
    "dev:game": "osfui dev --game",
    "check": "osfui check",
    "build": "npm run build:papyrus && npm run build:view",
    "build:papyrus": "pwsh -NoProfile -File ./build-papyrus.ps1",
    "build:view": "osfui build",
    "doctor": "osfui doctor"
  },
  "devDependencies": {
    "@osfui/cli": "__OSFUI_CLI_SPEC_JSON__"
  }
}
