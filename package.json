{
  "name": "osfui",
  "version": "0.0.0",
  "private": true,
  "description": "OSF UI runtime and built-in views.",
  "license": "GPL-3.0-or-later",
  "engines": {
    "node": ">=20.19"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace osfui-frontend",
    "check:terminology": "node scripts/check-terminology.mjs",
    "test": "npm test --workspace osfui-frontend",
    "verify": "npm run check:terminology && npm run verify --workspace osfui-frontend"
  }
}
