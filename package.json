{
  "name": "osfui",
  "version": "0.0.0",
  "private": true,
  "description": "Root convenience scripts. The actual frontend lives in frontend/.",
  "license": "GPL-3.0-or-later",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:view": "node scripts/view-harness.mjs",
    "test:view-harness": "node --test scripts/view-harness.test.mjs",
    "build": "npm --prefix frontend run build",
    "test": "npm run test:view-harness && npm --prefix frontend test",
    "verify": "npm run test:view-harness && npm --prefix frontend run verify"
  }
}
