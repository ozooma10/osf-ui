{
  "name": "osfui",
  "version": "2.0.0",
  "private": true,
  "description": "OSF UI WebView runtime and view-authoring CLI.",
  "license": "GPL-3.0",
  "engines": {
    "node": ">=20.19"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace osfui-frontend",
    "test": " npm test --workspace osfui-frontend",
    "verify": "npm run verify --workspace osfui-frontend"
  }
}
