{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ozooma10/osf-ui/blob/main/docs/schema/manifest.schema.json",
  "title": "OSF UI 2.0 View Manifest",
  "description": "Web-view metadata for Data/SFSE/Plugins/OSF/UI/views/<mod-id>/<view-name>/manifest.json. Identity is derived from the folders. Unknown properties are ignored for forward compatibility.",
  "type": "object",
  "required": ["manifestVersion"],
  "additionalProperties": true,
  "properties": {
    "$schema": { "type": "string" },
    "manifestVersion": { "const": 1 },
    "mod": {
      "type": "string",
      "description": "Optional authoring consistency hint; identity is always derived from the containing folder."
    },
    "title": { "type": "string" },
    "description": { "type": "string" },
    "launcher": {
      "type": "object",
      "description": "Opt this menu into the OSF Settings launcher. Omit for private and HUD views.",
      "required": ["modId"],
      "additionalProperties": false,
      "properties": {
        "modId": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": "^[a-z0-9._-]+$", "not": { "enum": [".", ".."] } },
        "modTitle": { "type": "string", "maxLength": 256 }
      }
    },
    "entry": {
      "type": "string",
      "default": "index.html",
      "description": "Relative entry HTML path. Absolute paths and '..' components are rejected."
    },
    "width": { "type": "integer", "minimum": 1, "maximum": 16384, "default": 1600 },
    "height": { "type": "integer", "minimum": 1, "maximum": 16384, "default": 900 },
    "transparent": { "type": "boolean", "default": true },
    "kind": { "type": "string", "enum": ["menu", "hud"], "default": "menu" },
    "capturesInput": {
      "type": "boolean",
      "default": true,
      "description": "Menu-only. HUD views are always passive."
    },
    "pausesGame": {
      "type": "boolean",
      "default": true,
      "description": "Menu-only. HUD views never pause."
    },
    "openOnStart": {
      "type": "boolean",
      "default": false,
      "description": "Autostart applies only to HUD views."
    },
    "order": {
      "type": "integer",
      "default": 0,
      "description": "HUD paint order within OSF UI's layer band."
    },
    "debugOnly": {
      "type": "boolean",
      "default": false,
      "description": "A debug-only HUD autostarts only when osfui.developerMode is enabled in OSF Settings."
    }
  },
  "allOf": [{
    "if": { "required": ["launcher"] },
    "then": { "properties": { "kind": { "const": "menu" } } }
  }]
}
