const ADMIN_HTML = `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>OSF UI report artifacts</title>
  <style>
    :root{color-scheme:dark;--bg:#0a0d12;--panel:#111721;--panel2:#171f2b;--line:#283444;--text:#eef4fb;--muted:#91a0b3;--accent:#72d6c5;--danger:#ff7d86;font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at 15% 0,#142330 0,var(--bg) 32rem);color:var(--text)}main{width:min(1440px,calc(100% - 32px));margin:auto;padding:42px 0 64px}header{display:flex;align-items:start;justify-content:space-between;gap:24px;margin-bottom:28px}h1,h2,h3,p{margin-top:0}h1{margin-bottom:8px;font-size:clamp(2rem,5vw,3.2rem);letter-spacing:-.04em}h2{margin:0;font-size:1rem}h3{margin-bottom:8px;font-size:.78rem;letter-spacing:.09em;text-transform:uppercase;color:var(--muted)}.eyebrow{margin-bottom:8px;color:var(--accent);font-size:.75rem;font-weight:800;letter-spacing:.16em;text-transform:uppercase}.lede,.hint,.muted{color:var(--muted)}.lede{max-width:680px;line-height:1.55}.card{background:var(--panel);border:1px solid var(--line);border-radius:14px;box-shadow:0 18px 60px #0005}.login{max-width:560px;padding:24px}label{display:block;margin-bottom:8px;font-size:.82rem;font-weight:700}.login-row,.toolbar{display:flex;gap:10px}.login-row input{flex:1}input,button{min-height:42px;border-radius:8px;border:1px solid var(--line);font:inherit}input{min-width:0;padding:0 13px;background:#080c11;color:var(--text);outline:none}input:focus{border-color:var(--accent);box-shadow:0 0 0 3px #72d6c522}button{padding:0 15px;cursor:pointer;border-color:var(--accent);background:var(--accent);color:#07110f;font-weight:800}button:hover{filter:brightness(1.08)}button:disabled{opacity:.55;cursor:wait}button.secondary{border-color:var(--line);background:var(--panel2);color:var(--text)}.hint{margin:12px 0 0;font-size:.78rem}.error{min-height:1.2em;margin:10px 0 0;color:var(--danger)}.toolbar{margin-bottom:12px}.toolbar input{width:min(480px,100%)}.layout{display:grid;grid-template-columns:minmax(280px,360px) minmax(0,1fr);gap:12px;align-items:start}aside{overflow:hidden}.section-heading{display:flex;align-items:center;justify-content:space-between;padding:17px 18px;border-bottom:1px solid var(--line)}.count{color:var(--muted);font-size:.78rem}.report-list{max-height:calc(100vh - 235px);overflow:auto}.report{display:block;width:100%;height:auto;padding:15px 18px;border:0;border-bottom:1px solid var(--line);border-radius:0;background:transparent;color:var(--text);text-align:left;font-weight:400}.report:hover,.report.active{filter:none;background:var(--panel2)}.report.active{box-shadow:inset 3px 0 var(--accent)}.report-title{display:block;margin-bottom:7px;font-weight:750;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.meta{display:flex;align-items:center;flex-wrap:wrap;gap:7px;color:var(--muted);font-size:.72rem}.pill{padding:2px 7px;border:1px solid var(--line);border-radius:999px;text-transform:uppercase;letter-spacing:.05em}.pill.published{border-color:#3d776c;color:#8ce2d3}.pill.failed{border-color:#803e46;color:#ff9ba2}.more{width:calc(100% - 28px);margin:14px}.detail{min-height:420px;padding:24px;overflow:hidden}.empty{display:grid;min-height:180px;place-items:center;color:var(--muted);text-align:center}.detail-head{display:flex;justify-content:space-between;gap:18px;padding-bottom:20px;border-bottom:1px solid var(--line)}.detail h2{font-size:1.45rem;line-height:1.3}.detail-id{margin:8px 0 0;color:var(--muted);font:.74rem ui-monospace,monospace;word-break:break-all}.facts{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:1px;margin:20px 0;overflow:hidden;border:1px solid var(--line);border-radius:9px;background:var(--line)}.fact{padding:12px;background:var(--panel2)}.fact b{display:block;margin-top:4px;font-size:.9rem;word-break:break-word}.copy{min-height:32px;padding:0 10px;font-size:.72rem}.block{margin-top:22px}.prose,pre{margin:0;line-height:1.55;white-space:pre-wrap;overflow-wrap:anywhere}pre{max-height:430px;overflow:auto;padding:14px;border:1px solid var(--line);border-radius:9px;background:#080c11;color:#d7e3ef;font:.78rem/1.55 ui-monospace,"Cascadia Mono",monospace}.artifact{margin-top:10px}.artifact summary{cursor:pointer;padding:12px 14px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-weight:700}.artifact[open] summary{border-radius:9px 9px 0 0}.artifact pre{border-top:0;border-radius:0 0 9px 9px}a{color:var(--accent)}@media(max-width:780px){main{width:min(100% - 20px,700px);padding-top:24px}.layout{grid-template-columns:1fr}.report-list{max-height:360px}.facts{grid-template-columns:1fr}.detail-head{display:block}}
  </style>
</head>
<body><main>
  <header><div><p class="eyebrow">OSF UI · private reporting</p><h1>Report artifacts</h1><p class="lede">Inspect the diagnostics and logs players consented to upload. Reports expire after 30 days.</p></div><button id="sign-out" class="secondary" hidden>Lock</button></header>
  <section id="login" class="card login"><label for="token">Admin token</label><div class="login-row"><input id="token" type="password" autocomplete="current-password" autofocus><button id="unlock">Unlock reports</button></div><p class="hint">The token stays in this browser tab and is sent only to this Worker.</p><p id="login-error" class="error" role="alert"></p></section>
  <section id="workspace" hidden><div class="toolbar"><input id="search" type="search" placeholder="Filter by title, ID, version, or status" aria-label="Filter reports"><button id="refresh" class="secondary">Refresh</button></div><div class="layout"><aside class="card"><div class="section-heading"><h2>Reports</h2><span id="count" class="count"></span></div><div id="reports" class="report-list"></div><button id="more" class="secondary more" hidden>Load more</button></aside><article id="detail" class="card detail"><div class="empty">Select a report to inspect its artifacts.</div></article></div></section>
</main><script>
(()=>{'use strict';
  const $=id=>document.getElementById(id);
  const state={token:sessionStorage.getItem('osfui-report-token')||'',reports:[],cursor:'',selected:''};
  const login=$('login'),workspace=$('workspace'),token=$('token'),error=$('login-error'),list=$('reports'),detail=$('detail'),more=$('more'),count=$('count'),signOut=$('sign-out');
  function el(tag,className,text){const node=document.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=String(text);return node}
  function showLogin(message){login.hidden=false;workspace.hidden=true;signOut.hidden=true;error.textContent=message||'';token.value='';token.focus()}
  function formatDate(value){const date=new Date(value);return Number.isNaN(date.valueOf())?'Unknown date':date.toLocaleString()}
  function lock(message){state.token='';state.reports=[];state.cursor='';state.selected='';sessionStorage.removeItem('osfui-report-token');showLogin(message)}
  async function api(path){const response=await fetch(path,{headers:{authorization:'Bearer '+state.token},cache:'no-store'});if(response.status===401){lock('That admin token was not accepted.');throw new Error('unauthorized')}if(!response.ok)throw new Error('Request failed ('+response.status+')');return response.json()}
  function reportButton(report){const button=el('button','report'+(report.id===state.selected?' active':''));button.type='button';button.append(el('span','report-title',report.title||'Untitled report'));const meta=el('span','meta');meta.append(el('span','pill '+report.status,report.status),el('span','',report.kind),el('span','',report.target||'osf-ui'),el('span','',formatDate(report.receivedAt)));button.append(meta);button.addEventListener('click',()=>selectReport(report.id));return button}
  function renderList(){const query=$('search').value.trim().toLowerCase();const filtered=state.reports.filter(report=>[report.title,report.id,report.pluginVersion,report.status,report.kind,report.target].some(value=>String(value||'').toLowerCase().includes(query)));list.replaceChildren(...filtered.map(reportButton));count.textContent=filtered.length+(filtered.length===1?' report':' reports');more.hidden=!state.cursor||Boolean(query);if(!filtered.length)list.append(el('div','empty',query?'No matching reports.':'No reports found.'))}
  function section(title,value){const wrap=el('section','block');wrap.append(el('h3','',title),el('div','prose',value||'Not provided'));return wrap}
  function fact(label,value){const node=el('div','fact');node.append(el('span','muted',label),el('b','',value||'—'));return node}
  function copyButton(label,getValue){const button=el('button','secondary copy',label);button.type='button';button.addEventListener('click',async()=>{await navigator.clipboard.writeText(getValue());const old=button.textContent;button.textContent='Copied';setTimeout(()=>{button.textContent=old},1200)});return button}
  function renderDetail(report){detail.replaceChildren();const head=el('div','detail-head'),heading=el('div');heading.append(el('h2','',report.title||'Untitled report'),el('p','detail-id',report.id));head.append(heading,copyButton('Copy report ID',()=>report.id));detail.append(head);const facts=el('div','facts');facts.append(fact('Received',formatDate(report.receivedAt)),fact('Kind / status',report.kind+' · '+report.status),fact('Repository target',report.target||'osf-ui'),fact('OSF UI version',report.pluginVersion),fact('Client reference',report.clientId),fact('Artifacts',String((report.logs||[]).length)+' log'+((report.logs||[]).length===1?'':'s')),fact('GitHub issue',report.issueNumber?'#'+report.issueNumber:'Not published'));detail.append(facts,section('Description',report.description),section('Reproduction',report.reproduction));if(report.issueUrl){const issue=el('section','block');issue.append(el('h3','','Public issue'));const link=el('a','',report.issueUrl);link.href=report.issueUrl;link.target='_blank';link.rel='noreferrer';issue.append(link);detail.append(issue)}const diagnostics=el('section','block');diagnostics.append(el('h3','','Diagnostics'),el('pre','',JSON.stringify(report.diagnostics??{},null,2)));detail.append(diagnostics);const artifacts=el('section','block');artifacts.append(el('h3','','Log artifacts'));if(!report.logs?.length)artifacts.append(el('p','muted','No log artifacts were included.'));for(const log of report.logs||[]){const item=el('details','artifact'),summary=el('summary','',log.name+(log.truncated?' · truncated':''));item.append(summary,el('pre','',log.content));artifacts.append(item)}detail.append(artifacts)}
  async function selectReport(id){state.selected=id;renderList();detail.replaceChildren(el('div','empty','Loading report…'));try{renderDetail(await api('/v1/reports/'+encodeURIComponent(id)))}catch(caught){if(String(caught.message)!=='unauthorized')detail.replaceChildren(el('div','empty',caught.message))}}
  async function load(reset){if(reset){state.reports=[];state.cursor='';state.selected=''}more.disabled=true;try{const query=state.cursor?'?cursor='+encodeURIComponent(state.cursor):'';const result=await api('/v1/reports'+query);state.reports.push(...result.reports);state.cursor=result.cursor||'';renderList()}catch(caught){if(String(caught.message)!=='unauthorized')list.replaceChildren(el('div','empty',caught.message))}finally{more.disabled=false}}
  async function unlock(){const candidate=token.value.trim();if(!candidate){error.textContent='Enter the configured admin token.';return}state.token=candidate;sessionStorage.setItem('osfui-report-token',candidate);login.hidden=true;workspace.hidden=false;signOut.hidden=false;await load(true)}
  $('unlock').addEventListener('click',unlock);token.addEventListener('keydown',event=>{if(event.key==='Enter')unlock()});$('refresh').addEventListener('click',()=>load(true));more.addEventListener('click',()=>load(false));$('search').addEventListener('input',renderList);signOut.addEventListener('click',()=>lock());if(state.token){login.hidden=true;workspace.hidden=false;signOut.hidden=false;load(true)}
})();
</script></body></html>`;

const SECURITY_HEADERS = {
  'cache-control': 'no-store',
  'content-security-policy': "default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; connect-src 'self'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'",
  'cross-origin-resource-policy': 'same-origin',
  'referrer-policy': 'no-referrer',
  'x-content-type-options': 'nosniff',
  'x-frame-options': 'DENY',
};

export function adminPage(): Response {
  return new Response(ADMIN_HTML, { headers: { ...SECURITY_HEADERS, 'content-type': 'text/html; charset=utf-8' } });
}
