{
  "name": "osfui-reporting-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit --lib ES2022 --skipLibCheck && vitest run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260727.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "^4.114.0"
  }
}
